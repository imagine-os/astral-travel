{
  "name": "astral-travel",
  "version": "0.3.1",
  "private": true,
  "type": "module",
  "description": "A local-first memory and dreaming playground with traceable knowledge and isolated simulations.",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "astral": "./bin/astral.mjs"
  },
  "scripts": {
    "start": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check web/app.mjs && node --check web/theme.mjs && node --check web/explorer.mjs && node --check web/objects-3d.mjs && node --check web/objects-css3d.mjs && node --check web/memory-model.mjs && node --check lib/core.mjs",
    "demo": "node bin/astral.mjs init --demo"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/imagine-os/astral-travel.git"
  }
}
